export interface Supervisor {
    email: string;
    name: string,
    indexNumber: string;
}

export interface SupervisorAvailability {
    name: string,
    taken: number,
    availability: number,
}
